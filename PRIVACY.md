# 隐私说明

抖音精读没有开发者运营的服务器、登录账号、广告、统计追踪或群消息发送。

- 抖音页面适配只在 www.douyin.com 运行，读取当前详情页的标题、可确认的作者、播放器、字幕及公开媒体地址。页面内桥接仅提取与当前视频编号一致的播放器公开资料，不读取其他页面账户资料。
- 页面脚本没有权限读取扩展存储。仅能通知页面切换、请求打开侧栏，或请求保存当前时间点。保存时间与原话由扩展核对当前播放器和本地缓存；页面只收到成功状态，不会取得密钥、已有笔记或触发付费服务。
- 火山语音仅在点击生成时收到当前节目的完整公开音轨，返回文字、时间点和说话人编号；任务 UUID 和所选识别服务保存在本机，查询复用同一任务。地址不写入缓存或日志。
- Supadata 仅在你点击“生成逐字稿”时收到标准化视频链接或公开媒体地址。签名媒体地址可能属于临时访问链接，传给服务用于本次转写，不写入缓存、笔记或日志。任务编号会在本地保存，便于稍后查询。
- 当前选中的 DeepSeek 官方或火山方舟在生成概览时收到当前视频逐字稿与相关标题、描述；解释仅发送选中文字与邻近上下文。没有翻译调用。
- 密钥保存在 Chrome 扩展本地存储中，不同步到开发者。该存储不等同于独立加密保险箱，请保护设备和浏览器账户。
- 逐字稿、概览、阅读位置最多保存最近 10 个视频。笔记单独保留，最多 1000 条，可逐条删除或导出。
- 新增“我的资料”在本扩展独立的IndexedDB中归档已有文字、标题、时间、说话人显示和笔记，不跟随最近10条缓存自动淘汰。归档不包括密钥、设置、音频地址、音频文件或识别任务记录；不向外部服务发送数据。已被淘汰的旧全文不能恢复。
- 资料文件夹由用户在Chrome选择并授权，文件夹句柄及本应用导出的相对路径保存在扩展IndexedDB。仅在用户点击保存时写入该文件夹的本平台子目录；可读取本应用此前记录的导出文件，不扫描其他目录。相同内容不重复写，内容变化另存副本，不覆盖旧文件。外部文件不会随缓存清理或卸载而删除。
- 设置页可清除逐字稿缓存，保留笔记、密钥及转写任务。卸载扩展会清除其本地数据，请先导出需保留的笔记。
- 导出文件由你决定保存位置与分享对象。时间点链接通过本插件的 dyd_t 参数实现。

权限：sidePanel 用于侧栏；storage 用于本地设置与阅读资料；tabs 用于定位当前视频；站点权限只包含抖音、Supadata API、火山语音 API、DeepSeek API 和火山方舟 API。没有历史记录、下载记录、摄像头或麦克风权限。

外部服务仍按自己的条款和隐私政策处理收到的数据。语音转写可能收费；网络中断或不支持的视频可能无法生成结果。重新提交会明确提示可能再次计费。

## 0.1.7 音频直传

点击生成后，插件在本机读取当前公开节目的 douyinvod.com 音轨，不读取浏览器 Cookie，约每五分钟转换为单声道音频发给火山极速识别。压缩媒体与音频只暂存在处理内存；成功文字、分段时间、任务编号保存在本机。没有本地伴随服务。关闭侧栏继续处理；关闭浏览器或重载插件会中断，成功部分保留，未知提交结果不自动重试。旧标准版任务仍使用原任务编号查询。


## 整期说话人候选
新源码保持原 AAC 音轨，一次交给火山标准识别，同时请求说话人分离；不读取画面做人脸判断、不建立跨视频声音身份库。完整音轨仅在本机处理内存和本次服务请求中使用；本机持久保存的是逐字稿、任务 ID、时长与声音分组。姓名是用户对本视频声音编号的可选命名。旧稿与已保存笔记不自动重写；旧分段任务仍可查询或读取。
